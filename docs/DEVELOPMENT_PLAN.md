# TIP输入法项目 AI Agent 开发全计划

> 适配 Windows 平台 | 基于 TSF 现代输入法框架 | AI Agent 全流程辅助开发 | 代码托管至 GitHub

---

## 一、项目总览

### 1.1 项目目标

开发一款轻量、开源、高性能的 Windows 桌面拼音输入法，基于微软官方 **TSF（Text Services Framework）** 现代框架实现，支持基础拼音输入、自定义短语、万级本地词库，兼顾输入流畅度与系统兼容性，全流程由 AI Agent 辅助完成编码、调试、文档与测试工作。

### 1.2 代码仓库

- **仓库地址：** `https://github.com/SpadyDong/tip-ime`
- **开源协议：** MIT 协议（宽松商用友好，无传染限制）
- **提交规范：** 采用 Git 语义化提交，分模块迭代合入主干

### 1.3 适配平台

- **系统支持：** Windows 10 21H2+ / Windows 11 全版本（低于 21H2 的 Win10 不做主动适配，随缘可用）
- **架构支持：** x86（32 位）、x64（64 位）双架构编译
- **兼容场景：** 桌面软件、UWP 应用、常见 IDE / Office / 浏览器、高 DPI 多屏；全屏独占游戏仅做尽力适配，不纳入 V1.0 必达项

### 1.4 核心能力（V1.0 版本）

- 全拼音输入、简拼支持、基础模糊音适配
- 本地词库支持 10000+ 词条，输入检索延迟 < 50ms
- 跟随光标候选栏、数字选词、翻页、基础皮肤
- 中英文切换、全半角切换、中英文标点切换
- 独立设置面板、自定义短语导入导出
- 标准安装包，支持一键注册 / 卸载输入法

---

## 二、整体架构设计

### 2.1 三层架构总览

采用「系统适配层 + 业务逻辑层 + UI 表现层」分层架构，层间通过接口解耦，便于 AI Agent 分模块并行开发。

```text
┌─────────────────────────────────────────────────┐
│              UI 表现层 (Win32/Direct2D)         │
│  候选栏窗口 | 设置面板 | 状态栏图标 | 软键盘    │
├─────────────────────────────────────────────────┤
│              业务逻辑层 (纯C++无系统依赖)       │
│  拼音引擎 | 词库管理 | 配置管理 | 输入状态机    │
├─────────────────────────────────────────────────┤
│              TSF 系统适配层 (COM组件)           │
│  键盘事件拦截 | 文本提交 | 上下文管理 | COM注册 │
└─────────────────────────────────────────────────┘
```

### 2.2 各层详细设计

#### 2.2.1 TSF 系统适配层（内核层）

输入法与 Windows 系统交互的核心，以 COM DLL 形式运行，由 TSF 框架按需加载到各应用进程内；`ctfmon.exe` 主要负责语言栏与输入处理器的启动协调，不承载输入法主逻辑。

**核心接口实现：**

- `ITfTextInputProcessor`：输入法服务入口
- `ITfKeyEventSink`：键盘事件拦截与处理
- `ITfCompositionSink` / `ITfComposition`：输入组合串管理
- `ITfThreadMgrEventSink`：线程上下文生命周期管理
- `ITfTextLayoutSink`：候选栏坐标跟随

**核心职责：**

- 接收系统键盘消息
- 上报候选文本
- 提交最终文字
- 同步输入光标坐标

#### 2.2.2 输入业务逻辑层

输入法核心算法与业务逻辑，与系统 API 解耦，可单独测试与移植。

- **拼音解析引擎：** 拼音切分、简拼匹配、多音字处理、模糊音映射
- **词库检索引擎：** Trie 前缀树索引 + 哈希快速匹配，支持万级词条低延迟检索
- **词库管理模块：** 基础词库加载、用户自定义词增删、词频动态更新
- **配置管理模块：** 读写配置文件、快捷键映射、输入偏好设置
- **输入状态机：** 中英文状态、全半角、标点模式切换

#### 2.2.3 UI 交互表现层

负责所有可视化交互，采用 Win32 原生窗口 + Direct2D 渲染，保证全场景兼容。

- **候选栏窗口：** 跟随输入光标、悬浮置顶、无焦点抢占、多列候选布局
- **状态栏图标：** 托盘图标、输入状态指示、快捷切换入口
- **设置面板：** 独立 Win32 窗口程序，提供可视化配置界面
- **皮肤渲染：** 支持基础配色自定义、高 DPI 自适应

### 2.3 核心数据流

1. 系统键盘消息 → TSF 层拦截 → 转发至业务层拼音引擎
2. 拼音引擎匹配词库 → 生成候选词列表 → 回传 UI 层渲染候选栏
3. 用户选词 / 上屏 → TSF 层提交文本至系统输入框
4. 用户配置修改 → 配置层持久化 → 实时更新输入行为

---

## 三、代码架构与文件夹结构

### 3.1 根目录结构

```text
tip-ime/
├── src/                  # 核心源码（主开发目录）
├── third_party/          # 第三方依赖库（开源无传染）
├── data/                 # 资源文件：词库、皮肤、配置模板
├── installer/            # 安装包脚本与打包配置
├── tests/                # 单元测试、集成测试用例
├── docs/                 # 开发文档、设计文档、使用说明
├── scripts/              # 编译脚本、自动化部署、CI配置
├── .github/              # GitHub Actions CI 工作流
├── LICENSE               # MIT 开源协议
└── README.md             # 项目说明文档
```

### 3.2 核心源码目录详解

```text
src/
├── core/                 # TSF 内核层（COM 组件实现）
│   ├── dllmain.cpp       # DLL 入口、COM 类工厂
│   ├── TextService.h/cpp # 主文本服务类，实现核心TSF接口
│   ├── KeyEventSink.h/cpp# 键盘事件接收器
│   ├── Compartment.h/cpp # 输入状态隔间管理
│   └── ComRegister.h/cpp # COM 组件注册/注销逻辑
├── engine/               # 输入业务引擎层
│   ├── pinyin/           # 拼音解析模块：切分、模糊音、简拼
│   ├── dictionary/       # 词库模块：Trie索引、词条加载、检索
│   ├── config/           # 配置管理：读写ini、偏好设置
│   └── state/            # 输入状态机：中英文、全半角切换
├── ui/                   # UI 表现层
│   ├── candidate/        # 候选栏窗口实现
│   ├── setting/          # 设置面板独立程序
│   ├── tray/             # 托盘图标与状态指示
│   └── render/           # Direct2D 渲染基础封装
├── utils/                # 通用工具库
│   ├── string_utils.h    # 字符串编码转换（GBK/UTF-16/UTF-8）
│   ├── logger.h          # 日志输出（调试排错核心）
│   └── thread_pool.h     # 异步检索线程池
└── resource/             # 窗口资源、图标、manifest
```

---

## 四、分阶段开发流程与开发计划

单人 + AI Agent 协作模式，V1.0 正式可用版本总周期约 **16 天**（阶段 0–4 合计），预留 2 天缓冲用于回归测试与问题修复。

### 阶段 0：环境搭建与仓库初始化（1 天）

**目标：** 完成开发环境、仓库骨架、基础依赖准备。

- GitHub 仓库初始化，提交 `LICENSE`、`README`、基础目录结构
- 本地开发环境搭建：Visual Studio 2022、Windows SDK、Git
- AI Agent 生成项目编译配置：`CMakeLists.txt` / VS 工程文件
- 提交初始骨架代码，完成 CI 基础配置

**AI Agent 分工：**

- **代码生成 Agent：** 生成目录骨架、编译配置、基础工具类
- **文档 Agent：** 编写初始 README、开发环境搭建文档

### 阶段 1：TSF 最小内核原型（3 天）

**目标：** 实现可注册、可输入汉字的最小 TSF 输入法 DLL。

- 实现 COM 组件基础框架：类工厂、DLL 入口、注册注销逻辑
- 实现核心 TSF 接口：键盘拦截、组合串管理、文本上屏
- 极简候选逻辑：固定拼音映射，验证输入链路通断
- 完成 DLL 编译、系统注册、记事本输入验证

**交付物：** 可正常输入单个汉字的 TSF 输入法 DLL

**AI Agent 分工：**

- **代码生成 Agent：** 生成全套 TSF 接口样板代码、COM 注册逻辑
- **调试 Agent：** 协助定位注册失败、输入无响应等系统级报错

### 阶段 2：拼音输入引擎与词库系统（5 天）

**目标：** 完成完整拼音输入能力，支持万级词库流畅检索。

- 拼音解析引擎：全拼切分、简拼支持、基础模糊音
- 词库系统：Trie 前缀树索引实现、二进制词库加载、词条检索
- 导入基础词库（1 万条通用词汇），验证检索性能
- 用户自定义词功能：动态增删、词频更新、持久化存储

**交付物：** 可连续拼音输入、支持万级词条的输入引擎（可单元测试独立验证）

**AI Agent 分工：**

- **代码生成 Agent：** 生成 Trie 树、拼音解析、词库序列化代码
- **测试 Agent：** 生成词库检索性能测试、拼音匹配单元测试

### 阶段 3：候选 UI 与设置面板开发（4 天）

**目标：** 完成可视化交互，实现可用的输入体验。

- 候选栏窗口：跟随光标、数字选词、翻页、高 DPI 适配
- 输入状态栏：中英文 / 全半角状态指示、快捷切换
- 独立设置面板：可视化修改候选数量、模糊音开关、快捷键
- 自定义短语导入导出功能

**交付物：** 带完整交互 UI 的可用输入法

**AI Agent 分工：**

- **代码生成 Agent：** 生成 Win32 窗口、候选栏渲染、设置界面代码
- **调试 Agent：** 协助解决候选栏错位、焦点抢夺、高 DPI 缩放问题

### 阶段 4：兼容性优化与安装部署（3 天）

**目标：** 全场景兼容，提供标准安装包。

- 兼容性适配：UWP 应用、浏览器、Office、常见 IDE
- 32 位 / 64 位双架构编译验证
- 制作安装包：NSIS 脚本，实现一键安装、注册、卸载
- 基础异常处理：崩溃保护、日志上报、资源泄漏修复

**交付物：** 可发布的 V1.0 安装包

**AI Agent 分工：**

- **代码生成 Agent：** 生成 NSIS 安装脚本、异常捕获逻辑
- **测试 Agent：** 生成兼容性测试用例清单

### 阶段 5：AI 增强与迭代优化（按需）

**目标：** 拓展高阶能力，持续优化体验。

- 整句联想、上下文纠错
- 本地小模型接入（可选）
- 皮肤系统、自定义外观
- 更多输入方案（五笔、双拼）

### 总周期预估

| 阶段 | 周期 | MVP 定义 |
|------|------|----------|
| 最小可用 Demo | 4 天（阶段 0+1） | TSF DLL 可注册、可在记事本输入固定映射汉字 |
| V1.0 正式可用版本 | 16 天（含 2 天缓冲） | 完整拼音输入 + 候选栏 + 设置面板 + 安装包 |
| 持续迭代优化 | 长期按需迭代 | 整句联想、皮肤、更多输入方案 |

---

## 五、AI Agent 分工与协作开发计划

### 5.1 Agent 角色划分

设置 4 类专项 Agent，各司其职，并行提升开发效率。

| Agent 角色 | 核心职责 | 输出产物 |
|------------|----------|----------|
| 代码生成 Agent | 按模块生成 C++ 源码、接口实现、工具类、编译脚本 | 可编译的功能代码、头文件、工程配置 |
| 调试排错 Agent | 分析编译报错、运行时崩溃、系统兼容问题，给出修复方案 | 问题根因分析、修复代码片段、排查步骤 |
| 测试验证 Agent | 生成单元测试用例、性能测试脚本、兼容性测试清单 | 测试代码、测试报告、验收标准 |
| 文档运维 Agent | 编写开发文档、README、使用说明、仓库维护、CI 配置 | 技术文档、发布说明、自动化脚本 |

### 5.2 Agent 协作流程

1. **需求拆解：** 将阶段目标拆分为单个模块任务
2. **代码生成：** 代码生成 Agent 输出初始代码，人工确认架构合理性
3. **编译调试：** 人工编译，报错信息输入调试 Agent，迭代修复至编译通过
4. **测试验证：** 测试 Agent 生成用例，验证功能正确性与性能指标
5. **文档同步：** 文档 Agent 同步更新对应模块开发文档
6. **代码提交：** 通过评审后合入仓库对应分支

### 5.3 AI 辅助开发核心落地点

- **TSF 样板代码生成：** 替代人工查阅晦涩的微软文档，快速生成 COM 接口框架，减少 70% 底层样板代码工作量
- **报错快速定位：** 系统级 COM 报错、注册失败、输入无响应等问题，AI 基于知识库快速给出排查路径
- **算法代码生成：** Trie 树、拼音切分、词库序列化等标准化算法，AI 一键生成可运行代码
- **测试用例自动生成：** 针对拼音引擎、词库检索自动生成边界测试用例，覆盖人工遗漏场景
- **文档自动同步：** 代码迭代后自动更新对应文档，减少人工维护成本

---

## 六、验证测试计划

### 6.1 单元测试

覆盖核心业务逻辑，确保底层算法正确性。

- **测试范围：** 拼音解析、词库检索、配置读写、字符串工具
- **测试框架：** Google Test（gtest）
- **验收标准：** 核心模块代码覆盖率 ≥ 80%，所有用例全部通过
- **执行时机：** 每个模块开发完成后执行，合入主干前必跑

### 6.2 集成测试

验证全链路输入流程通畅。

**测试场景：**

- 全拼音输入、简拼输入、模糊音输入
- 候选翻页、选词、上屏、删除回退
- 中英文切换、全半角切换、标点切换
- 自定义词增删、导入导出

**验收标准：** 全流程无崩溃、无乱码、文本上屏正确

### 6.3 兼容性测试（Windows 平台专项）

针对 Windows 多场景做专项适配验证。

| 测试分类 | 测试场景 | 验收标准 |
|----------|----------|----------|
| 系统版本 | Win10 21H2 / Win11 22H2 / Win11 24H2 | 均可正常安装注册、输入正常 |
| 软件兼容 | 记事本、Chrome/Edge 浏览器、VS Code、Office、WPS | 候选栏跟随正常、输入无卡顿 |
| 特殊场景 | UWP 应用、全屏游戏、远程桌面、虚拟机 | 输入法可正常唤起、候选不错位 |
| 显示适配 | 100%/125%/150%/200% 缩放、双显示器不同 DPI | 候选栏位置准确、无模糊 |
| 架构兼容 | 32 位程序、64 位程序 | 双架构输入法均正常工作 |

### 6.4 性能测试（词库与输入流畅度）

针对万级词库做性能兜底验证。

**测试指标：**

- 冷启动加载耗时：1 万词条加载 ≤ 300ms（SSD）
- 单次检索延迟：平均拼音匹配延迟 ≤ 50ms
- 内存占用：常驻内存 ≤ 50MB
- CPU 占用：连续输入时 CPU 占用 ≤ 5%

**测试环境：** Intel Core i5-1135G7 / 16GB 内存 / NVMe SSD / Windows 11 23H2；该环境为性能基线，低于此配置的机器作为参考项而非阻塞项。

**验收标准：** 全部指标达标，无肉眼可感知卡顿

### 6.5 回归测试流程

每次版本迭代后执行全量核心用例回归，确保旧功能不被破坏。

- **核心回归用例集：** 基础输入、系统注册、常见软件兼容
- **触发时机：** 每次合入主干、发布版本前
- **输出产物：** 版本测试报告、已知问题清单

---

## 七、开发注意事项（Windows 输入法专项踩坑指南）

### 7.1 TSF COM 开发核心注意事项

- **调试方式受限：** TSF DLL 被加载到各应用进程内，Visual Studio 可通过附加目标进程调试，但日志仍是排错主力，项目初期务必完善日志系统
- **COM 注册权限：** 注册输入法需要管理员权限写入注册表，安装包必须申请管理员权限
- **线程模型：** 根据注册表 `ThreadingModel` 选择 STA / MTA / Free；跨线程 COM 调用必须正确列集，避免随机崩溃
- **引用计数管理：** COM 接口引用计数错误会导致内存泄漏、进程无法退出，AI 生成代码需人工核对 AddRef/Release 配对
- **上下文生命周期：** 切换输入框时输入上下文会重建，必须正确处理销毁事件，避免悬空指针崩溃

### 7.2 词库性能注意事项

- 禁止使用线性遍历匹配词条；万级词条采用 **Trie 前缀树** 索引，拼音前缀匹配时间复杂度约为 O(m)（m 为拼音长度），可稳定控制在 10ms 以内
- 词库检索是否异步视实测结果而定：Trie 索引下万级检索通常无需异步即可满足 <50ms 指标；若后续词库规模扩大或引入整句联想，再考虑异步线程池
- 词库文件采用二进制序列化存储，避免启动时逐行解析文本，大幅提升加载速度
- 高频词与低频词分层存储，优先匹配高频词，进一步降低检索耗时

### 7.3 系统兼容注意事项

- **高 DPI 适配：** 候选窗口必须启用 Per-Monitor DPI 感知，通过系统 API 获取光标坐标，禁止硬编码像素位置
- **UWP 沙箱兼容：** UWP 应用权限隔离，无法使用部分窗口消息，必须通过 TSF 标准接口获取光标位置
- **全屏游戏兼容：** 全屏独占模式（Fullscreen Exclusive）会绕过 DWM，普通置顶窗口可能无法显示；V1.0 仅做尽力适配，不作为必达项
- **32/64 位双编译：** 系统存在 32 位程序时，必须同时部署 32 位输入法 DLL，否则 32 位软件无法调用输入法

### 7.4 部署与安全注意事项

- **数字签名：** 正式发布版本建议加代码签名证书，避免被 Windows Defender 拦截为恶意软件
- **卸载清理：** 卸载时必须完整清理注册表项、DLL 文件、用户数据，避免残留导致系统输入法异常
- **权限最小化：** 输入法运行时无需管理员权限，仅安装注册时需要，避免高权限带来的安全风险
- **异常保护：** 核心逻辑增加 SEH 异常捕获，避免输入法崩溃导致宿主应用一起崩溃

### 7.5 开源合规注意事项

- 第三方依赖严格遵循开源协议，避免引入 GPL 强传染组件
- 引用开源代码需保留原版权声明，放入对应目录的 license 文件
- 词库资源采用开源词库（如雾凇拼音词库），确保无版权风险
- 项目整体采用 MIT 协议，允许商用修改与分发

---

## 八、仓库管理与交付规范

### 8.1 分支管理

- `main`：主干分支，存放稳定可发布版本
- `dev`：开发分支，日常迭代合入
- `feature/xxx`：功能分支，单个功能开发完成后合入 dev
- `release/vx.x.x`：发布分支，用于版本发布与修复
- `hotfix/xxx`：紧急问题修复分支

> **合入规范：** `v0.x` 阶段所有代码变更仅允许合入 `v0.x` 系列最新版本分支（当前为 `v0.1`），禁止直接合入 `main`。该阶段可直接向 `v0.x` 最新版本分支推送代码，无需审核。

### 8.2 提交规范

采用语义化提交信息，格式：`类型(模块): 提交描述`

**类型：**

- `feat`（新功能）
- `fix`（修复）
- `docs`（文档）
- `refactor`（重构）
- `test`（测试）
- `chore`（工程配置）

**示例：** `feat(dictionary): 新增 Trie 树词库检索实现`

### 8.3 版本发布流程

1. 从 `dev` 切出 `release` 分支
2. 执行全量测试与回归，修复发布前问题
3. 更新版本号、更新日志、发布说明
4. 合入 `main` 分支，打 tag 标签
5. 编译打包安装包，上传至 GitHub Release

---

## 九、风险与应对

### 9.1 技术风险

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| TSF 接口调试困难，注册/上屏失败 | 阶段 1 延期 | 优先建立日志系统；每实现一个接口即在记事本验证；复杂问题拆分为最小可复现步骤 |
| 词库检索性能不达标 | 阶段 2 返工 | 先用 1 万词条实测；若 Trie 索引仍不达标，再引入分层索引或异步检索 |
| 候选栏高 DPI / 多屏坐标错位 | 阶段 3 延期 | 使用 `ITfTextLayoutSink` 获取光标坐标；在不同 DPI 显示器上实测 |
| 32/64 位兼容问题 | 阶段 4 阻塞 | 从阶段 0 起即配置双架构 CI；关键 COM 代码避免架构相关假设 |

### 9.2 协作风险

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| AI 生成代码质量不稳定 | 返工增加 | 每个模块必须人工 Review；核心 COM/TSF 代码人工复核 AddRef/Release 和资源生命周期 |
| 阶段边界不清晰导致并行冲突 | 合并困难 | 按层划分模块，层间接口先行约定；每日同步关键接口变更 |
| 测试环境不足 | 兼容性问题遗漏 | 至少准备一台 Win10 21H2 和一台 Win11 物理机或虚拟机做最终验证 |

---

## 十、资源与依赖

### 10.1 开发资源

- **硬件：** Windows 开发机 1 台（建议 Win11 23H2 + Win10 21H2 双系统或虚拟机）
- **软件：** Visual Studio 2022、Windows SDK 10.0.22000+、CMake 3.20+、Git、NSIS（安装包）
- **测试机/虚拟机：** 至少覆盖 x64 Win10 21H2、x64 Win11 23H2 两种环境

### 10.2 第三方依赖

| 依赖 | 用途 | 协议 | 备注 |
|------|------|------|------|
| Google Test | 单元测试 | BSD-3-Clause | 仅测试可执行文件链接，不进入输入法 DLL |
| 雾凇拼音词库（或其他开源词库） | 基础词库 | 以具体词库协议为准 | 需确认商用友好并保留版权声明 |

### 10.3 关键外部资源

- 微软 TSF 官方文档与示例代码
- 开源输入法参考实现（如 Weasel、PIME 等，仅作架构参考，不直接复制代码）

---

## 十一、V1.0 MVP 验收标准

### 11.1 阶段 0 验收

- [ ] GitHub 仓库已初始化，包含 `LICENSE`、`README`、基础目录结构
- [ ] Visual Studio / CMake 可编译空项目并通过 CI

### 11.2 阶段 1 验收（最小可用 Demo）

- [ ] DLL 可在管理员权限下注册为输入法
- [ ] 在记事本中切换输入法后，按固定拼音可上屏至少一个汉字
- [ ] 卸载脚本可清理注册表项

### 11.3 阶段 2 验收

- [ ] 支持全拼、简拼、至少 5 组基础模糊音
- [ ] 1 万词条词库加载 ≤ 300ms，检索 ≤ 50ms
- [ ] 单元测试覆盖拼音解析、词库检索、配置读写

### 11.4 阶段 3 验收

- [ ] 候选栏跟随光标，数字 1–9 选词，PageUp/PageDown 翻页
- [ ] 设置面板可修改候选数量、模糊音开关、快捷键
- [ ] 支持自定义短语导入导出

### 11.5 阶段 4 验收

- [ ] 在记事本、Chrome/Edge、VS Code、Office、WPS 中输入正常
- [ ] 100%/125%/150% DPI 下候选栏位置正确
- [ ] 32 位和 64 位安装包分别可安装、注册、卸载
- [ ] 无崩溃、无内存泄漏（通过 30 分钟连续输入测试）
